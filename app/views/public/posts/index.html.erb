<div class="container">
  <div class="row">
    <div class="col-md-10">
      <h2>投稿一覧(全<%= @post_all.count %>件)</h2>
        <% @posts.each do |post| %>
          <% if post.post_image.attached? %>
            <% post.post_image.each do |post_image| %>
              <%= image_tag postimage.variant(resize: '300x300') %>
            <% end %>
          <% end %>
          <%= post.title %>
          <%= post.body %>
          <%= post.tags %>
        <% end %>
    </div>
  </div>
</div>