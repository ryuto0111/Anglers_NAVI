<div class="container pb-5">
  <div class="row">
    <div class="col-md-2">
      <%= render 'admin/tagsearches/sidebar', tags: @tags %>
      <%= render 'admin/posts/search_form' %>
    </div>
      <%= render 'admin/posts/index', posts: @posts %>
  </div>
  <div class="offset-4 mb-5">
    <%= paginate @posts, theme: 'bootstrap-5' %>
  </div>
</div>